#include <common.h>

int main()
{
	link l = NULL;
	l == (Node*)malloc(sizeof(Node));

	if (l == NULL) {
		printf("\n failure");
		return 0;
	}
/*
	while (1) {
		system("cls");
		switch (MenuSelect()) {
			case 1:
				input(1);
				break;
			case 2:
				del(1);
				break;
			case 3:
				list(1);
				break;
			case 4:
				search(1);
				break;
			case 5:
				save(1);
				break;
			case 6:
				load(1);
				break;
			case 7:
				exit(0);
				break;
		}
	}
*/
	return 0;
}